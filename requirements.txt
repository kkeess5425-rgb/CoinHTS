# ── 핵심 비동기 ──────────────────────────────────────
aiohttp>=3.9.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
websockets>=12.0

# ── 웹 프레임워크 ─────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# ── 데이터 처리 ───────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
polars>=0.20.0
msgspec>=0.18.0

# ── 고성능 지표 ───────────────────────────────────────
numba>=0.59.0

# ── UI (데스크탑) ─────────────────────────────────────
PySide6>=6.6.0
pyqtgraph>=0.13.0
qasync>=0.27.0
PyOpenGL>=3.1.0

# ── 음성 알림 (선택) ──────────────────────────────────
# pyttsx3>=2.90
# gTTS>=2.5.0
# pygame>=2.5.0

# ── 최적화 ────────────────────────────────────────────
scipy>=1.12.0

# ── 캐시 (선택) ───────────────────────────────────────
# redis>=5.0.0

# ── 뉴스 ─────────────────────────────────────────────
feedparser>=6.0.0

# ── 개발 도구 ─────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
